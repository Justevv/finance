server:
  port: 8081

manager:
  schedule:
    send-sms:
      crone: 10000
      batch-size: 100
    send-email:
      crone: 10000
      batch-size: 100

spring:
  datasource:
    url: jdbc:postgresql://localhost/postgres
    username: sa
    password: sa
    driverClassName: org.postgresql.Driver
  data:
    redis:
      host: localhost
      port: 6379
      password: passWd
  kafka:
    ssl:
      enabled: false
    producer:
      topics:
        email-verification: ${KAFKA_PRODUCER_TOPIC_EMAIL_VERIFICATION:manager-email-verification}
        phone-verification: ${KAFKA_PRODUCER_TOPIC_PHONE_VERIFICATION:manager-phone-verification}
      bootstrap-servers: ${KAFKA_PRODUCER_BOOTSTRAP_SERVERS:localhost:9092}
      batch-size: 16384
      linger-ms: 1000
      ssl:
        key-password:
        key-store-location:
        key-store-password:
        trust-store-location:
        trust-store-password:
        security:
          protocol: SSL
    streams:
      auto-startup: true

  mail:
    host: smtp.yandex.ru
    port: 465
    username: test
    password: test
    properties:
      "mail.transport.protocol": smtp
      "mail.smtp.auth": true
      "mail.smtp.starttls.enable": true
      "mail.debug": false
      "mail.smtp.ssl.enable": true

  jpa:
    show-sql: false
    hibernate.ddl-auto: validate
    properties:
      hibernate:
        default_schema: test

  flyway:
    schemas: test

logging:
  level:
    root: INFO
    org.springframework.security: INFO
    org.springframework.web: INFO
    org.hibernate: INFO
    com.manager: DEBUG
    com.manager.finance.metric: DEBUG
    org:
      springframework:
        web:
          filter:
            CommonsRequestLoggingFilter: DEBUG

metric:
  executionTime: true

URI:
  application: http://localhost:8080